$input v_color0, v_texcoord0

#include "common.sh"

SAMPLER2D(t_diffuse, 0);
SAMPLER2D(t_color, 1);

void main()
{
	gl_FragColor = mix(texture2D(t_diffuse, v_texcoord0), v_color0, texture2D(t_color, v_texcoord0).a);
}
