$input v_texcoord0

#include "common.sh"

SAMPLER2D(t_diffuse, 0);

struct DirectionalLight
{
	vec4 direction;
};
uniform DirectionalLight u_directionalLight[1];

void main()
{
//u_modelView
	gl_FragColor = texture2D(t_diffuse, v_texcoord0);
}
